const revealElements=document.querySelectorAll(".reveal");const revealObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add("active")})},{threshold:.15});revealElements.forEach(el=>revealObserver.observe(el));const counters=document.querySelectorAll(".counter");const counterObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){const counter=entry.target;const target=Number(counter.dataset.target);let value=0;const step=Math.ceil(target/80);const update=()=>{value+=step;if(value>=target){counter.textContent=target}else{counter.textContent=value;requestAnimationFrame(update)}};update();counterObserver.unobserve(counter)}})},{threshold:.5});counters.forEach(counter=>counterObserver.observe(counter));